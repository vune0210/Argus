# Managed staging probe; mount its private token separately.
ARGUS_ENV=staging
ARGUS_CONTROL_PLANE_URL=https://api.example.invalid
ARGUS_PROBE_ID=managed-ap-northeast-1
ARGUS_REGION=ap-northeast-1
ARGUS_TOKEN_FILE=/run/secrets/argus-probe-token
ARGUS_CONCURRENCY=4
ARGUS_ALLOW_PRIVATE_TARGETS=false
